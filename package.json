{
  "name": "web-doom-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "DOOM ported to web with TypeScript",
  "packageManager": "bun@latest",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter \"./packages/*\" dev",
    "build": "bun run build:libs && bun run build:pages",
    "build:libs": "bun run --filter @web-doom/wad build && bun run --filter @web-doom/game-controller build && bun run --filter @web-doom/core build && bun run --filter @web-doom/wad-viewer build && bun run --filter @web-doom/texture-transformer build",
    "build:pages": "bun run --filter @web-doom/pages build",
    "typecheck": "tsgo --noEmit -p packages/wad && tsgo --noEmit -p packages/game-controller && tsgo --noEmit -p packages/web-doom && tsgo --noEmit -p packages/wad-viewer && tsgo --noEmit -p packages/texture-transformer && tsgo --noEmit -p packages/pages",
    "test": "bun run --filter \"./packages/*\" test",
    "lint": "bun run typecheck",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo node_modules bun.lockb",
    "example:controller": "bun run packages/game-controller/examples/transform-controller.ts",
    "example:controller:batch": "bun run packages/game-controller/examples/batch-transform.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "^7.0.0-dev.20251029.1",
    "happy-dom": "^20.0.10",
    "jsdom": "^27.0.1",
    "typescript": "^5.9.3",
    "vite-plugin-dts": "^4.5.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "@google/genai": "^1.28.0",
    "@resvg/resvg-js": "^2.6.2"
  }
}
